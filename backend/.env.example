DATABASE_URL=postgresql://postgres:postgres@localhost:5433/lumence

# Clerk dashboard -> API Keys
CLERK_SECRET_KEY=sk_test_xxx
# Optional: PEM public key from Clerk dashboard for networkless JWT verification.
# If omitted, the SDK fetches Clerk's JWKS remotely instead (fine for now).
# CLERK_JWT_KEY=
# Comma-separated list of frontend origins allowed to call this API
CLERK_AUTHORIZED_PARTIES=http://localhost:3000

OPENAI_API_KEY=sk-xxx
DAILY_CHAT_LIMIT=20
