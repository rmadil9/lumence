# Test Strategy

> Status: **skeleton.**

TODO(adil): what is unit / integration / e2e, and what we deliberately do NOT test.
Known hard-to-test surfaces needing a decision: whatever captures desktop and browser
activity (likely untestable in CI — decide what we accept), and LLM calls (mock at the
adapter boundary).
