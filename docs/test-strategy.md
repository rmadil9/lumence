# Test Strategy

> Status: **skeleton.**

TODO(adil): what is unit / integration / e2e, and what we deliberately do NOT test.
Known hard-to-test surfaces needing a decision: the GNOME Shell extension,
the browser extension, and OpenAI calls (mock at the adapter boundary).
