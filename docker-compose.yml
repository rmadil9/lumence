services:
  postgres:
    image: postgres:17
    restart: unless-stopped
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: lumence
    ports:
      - "5433:5432"
    volumes:
      - lumence_pgdata:/var/lib/postgresql/data

volumes:
  lumence_pgdata:
